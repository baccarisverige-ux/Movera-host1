import { HostShell } from '../components/HostShell.jsx'
import { useHostListings } from '../model/useHostListings.js'
export function HostListingsPage({ onNavigate }) { const [items, setItems] = useHostListings(); const archive = id => setItems(items.map(item => item.id === id ? { ...item, status: 'archived' } : item)); return <HostShell title="Mes annonces" testId="page-host-listings"><button data-testid="host-create-link" onClick={() => onNavigate('/host/listings/new')}>Créer une annonce</button><div className="host-list">{items.map(item => <article className="host-listing" key={item.id} data-testid={`host-listing-${item.id}`}><div><strong>{item.title}</strong><span>{item.price} TND/nuit · {item.status}</span></div><div><button onClick={() => onNavigate(`/host/listings/${item.id}/edit`)}>Modifier</button>{item.status !== 'archived' && <button onClick={() => archive(item.id)}>Archiver</button>}</div></article>)}</div></HostShell> }
